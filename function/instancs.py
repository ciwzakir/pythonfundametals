"""
Calling or invoking Functions
Function is a block of code that only runs when it is called by name.
A function can return data as a result. 
We can pass data as a parameter into the function. 
We must called a function outside the Scope of the function block
A Function called by its name
"""



def instrsNames(first_name, last_name): # first_name, last_name are the parameters. startwith small letter and seperates by comma
    print(first_name + " " + last_name)

instrsNames("Habibur", "Rahman") # "Habibur", "Rahman" are the arguments(Values) of the parameters of that function
instrsNames("Imran", "Kabir")
instrsNames("Zakir", "Hossen")
instrsNames("Afsana", "Mumo")
instrsNames("Zenifar", "Lopez")
